spring.application.name=ebuffet-backend

# Single Buffet Mode - ID do buffet unico desta instancia
ebuffet.buffet-id=${EBUFFET_BUFFET_ID:1}

# File Upload Limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Cloudinary
cloudinary.cloud_name=dd7ycrswb
cloudinary.api_key=584512881426379
cloudinary.api_secret=ZlHAKuL0qd-vrevMSyc0hzlK9mo

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/ebuffet
spring.datasource.username=postgres
spring.datasource.password=admin

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Migrations/Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.table=versao_database

# Email SMTP (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:ebuffetapp@gmail.com}
spring.mail.password=${MAIL_PASSWORD:yeny rljr blos kwse}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
